# Experiments

`compare_distance_methods.py` computes absolute/relative errors and produces a
Homography-versus-Depth chart from built-in historical values or a supplied
CSV. `reference_error_chart.png` is the chart retained from the original
experiment.
